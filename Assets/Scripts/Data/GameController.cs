using UnityEngine;
using System.Collections;
using UnityEngine.UI;


public class GameController : MonoBehaviour {


	/*
	public static Building CrateBulding(string path, Vector3 position, Quaternion rotation,  string name){
		GameObject prefab = Resources.Load<GameObject> (path);
		GameObject go = GameObject.Instantiate (prefab, position, rotation) as GameObject;
		Building b = go.GetComponent<Building> ?? go.AddComponent<Building>;
		return b;
	}

	public static Building CrateBulding(BuildingData data, string path, Vector3 position, Quaternion rotation,  string name){
		GameObject prefab = Resources.Load<GameObject> (path);
		GameObject go = GameObject.Instantiate (prefab, position, rotation) as GameObject;
		Building b = go.GetComponent<Building> ?? go.AddComponent<Building>;
		Building.data = data;
		return b;
	}
	*/

}

